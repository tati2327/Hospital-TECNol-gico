import { EvaluacionServicio } from './evaluacion-servicio';

describe('EvaluacionServicio', () => {
  it('should create an instance', () => {
    expect(new EvaluacionServicio()).toBeTruthy();
  });
});
